{
  "name": "reaper",
  "version": "0.1.1",
  "description": "Solana distressed-collateral hunter that scores liquidation edge, oracle drift, and keeper-race probability",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.4"
  },
  "keywords": [
    "solana",
    "liquidations",
    "distressed-flow",
    "keepers"
  ],
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.2.0"
  }
}
