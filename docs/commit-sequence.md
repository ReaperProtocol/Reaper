# Reaper Commit Sequence

1. `feat(config): add oracle drift, oracle age, and liquidation edge thresholds`
2. `feat(types): expand positions with keeper race, unwind quality, and liquidation edge`
3. `feat(protocols): estimate distressed-flow edge for Kamino and MarginFi accounts`
4. `feat(scanner): rank positions by edge-adjusted keeper probability`
5. `feat(console): print liquidation desk metrics instead of plain health bars`
6. `feat(agent): rewrite prompt around forced-flow capture logic`
7. `test(positions): cover oracle and liquidation-edge fields`
8. `docs(readme): reposition repo as a liquidation hunter`
9. `design(assets): replace dashboard with high-speed distressed-flow console`
10. `chore(copy): align env and package metadata with keeper terminology`
